<div class="row mt-3">
  <% @artists.each do |artist| %>
  <div class="col-sm-6 col-md-4 col-lg-3 mb-3">
    <div class="card">
      <img src="<%= artist.image_url %>" class="card-img-top" alt="<%= artist.name%>">
      <div class="card-body">
        <h5 class="card-title text-center"><strong><%= artist.name %></strong></h5>
        <%= link_to "Ver más", artist_path(artist), class: 'btn btn-primary btn-block', target: '_blank' %>
      </div>
    </div>    
  </div>
  <% end %>
</div>
